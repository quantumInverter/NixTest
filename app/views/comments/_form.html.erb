<%= form_for [@question, Comment.new] do |f| %>
  <div class="actions">

    <div class="field">
      <%= f.label :content %><br/>
      <%= f.text_area :content %>
    </div>

    <div class="actions">
      <%= f.submit "Submit" %>
    </div>
  </div>
<% end %>
